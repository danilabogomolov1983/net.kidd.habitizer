namespace Net.Kidd.Habitizer.Shared.Kernel.Functional;

public static class ListExtensions
{
    // extension<A>(Task<IEnumerable<A>> that)
    // {
    //     public Task<IEnumerable<B>> MapAsync<B>(Func<A, B> func) =>
    //         that
    //             .Bind(list => list.Map(func).AsTask());
    // }
}

